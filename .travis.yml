language: node_js
node_js:
  - "node"
  - "lts/*"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
after_script:
  - npm run test:coverage
  - npm install coveralls && cat ./coverage/lcov.info | coveralls
